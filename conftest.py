# This (empty) file marks the project root for pytest. Its presence puts this
# folder on Python's import path, so tests can do `from services.user_service
# import app` and `from gateway.main import app` — the same module paths uvicorn
# uses. No code needed here; pytest just needs the file to exist.
